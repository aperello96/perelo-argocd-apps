---
apiVersion: v1
kind: Namespace
metadata:
  name: data-analys

---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airbyte
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: data-analys
  source:
    repoURL: https://airbytehq.github.io/helm-charts
    chart: airbyte
    targetRevision: 1.9.2
    helm:
      releaseName: airbyte
      values: |
        global:
          edition: community
          airbyteUrl: https://airbyte.apps.okd.pro.perelohome.com
          auth:
            enabled: false
          env_vars:
            # Temporary lower resource requests to improve scheduling in constrained clusters.
            JOB_MAIN_CONTAINER_CPU_REQUEST: "500m"
            JOB_MAIN_CONTAINER_CPU_LIMIT: "1"
            JOB_MAIN_CONTAINER_MEMORY_REQUEST: "1Gi"
            JOB_MAIN_CONTAINER_MEMORY_LIMIT: "1Gi"
            SIDECAR_KUBE_CPU_REQUEST: "250m"
            SIDECAR_KUBE_CPU_LIMIT: "500m"
            SIDECAR_KUBE_MEMORY_REQUEST: "1Gi"
            SIDECAR_KUBE_MEMORY_LIMIT: "1Gi"
            JOB_KUBE_INIT_CONTAINER_CPU_REQUEST: "500m"
            JOB_KUBE_INIT_CONTAINER_CPU_LIMIT: "1"
            JOB_KUBE_INIT_CONTAINER_MEMORY_REQUEST: "1Gi"
            JOB_KUBE_INIT_CONTAINER_MEMORY_LIMIT: "1Gi"

        ingress:
          enabled: false

        postgresql:
          enabled: true
          podSecurityContext: {}
          containerSecurityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          commonAnnotations: {}

        minio:
          enabled: true
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: airbyte
  namespace: data-analys
spec:
  host: airbyte.apps.okd.pro.perelohome.com
  to:
    kind: Service
    name: airbyte-airbyte-server-svc
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
