---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mongodb-password
  namespace: tripted-pro
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: mongodb-password
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: tripted/pro/mongodb
        property: admin-user
